[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: Fast isolated tests (no Engine process, no Docker).
    integration: CLI against repo Engine tree (filesystem + validation).
    e2e: Live Engine reachable at SEYOAWE_ENGINE (host:port); requires Docker stack in CI.
